// Nebula is a Tauri desktop SPA: no SSR, no prerender. Everything runs
// client-side + in Workers + Rust commands (svelte.config.js adapter-static, SPA fallback).
export const ssr = false;
export const prerender = false;
