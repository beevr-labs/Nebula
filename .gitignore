# dependencies
node_modules/
src-tauri/target/

# build output
build/
.svelte-kit/
dist/

# model weights cache (large, downloaded at runtime — NEVER commit)
/models/
.models-cache/
*.bin
*.onnx
*.wasm.cache

# local app data / vaults (never commit user content)
*.nebula/
NebulaVault/

# os / editor / agent session data (never commit)
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# logs & benches
*.log
tests/bench/out/

# DO commit: package-lock.json, Cargo.lock, models.manifest.json (hashes, not weights)
